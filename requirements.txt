# 防窺工坊 — Streamlit app (app.py), deployed on Streamlit Cloud.
# Streamlit Cloud always reads requirements.txt, so this file is the Streamlit
# app's deps. The FastAPI backend (main.py) uses requirements-api.txt instead.
streamlit>=1.41
streamlit-js-eval>=0.1.7
Pillow>=10.0
numpy>=1.24
tzdata>=2024.1
